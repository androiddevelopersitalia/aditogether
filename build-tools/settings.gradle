rootProject.name = 'build-tools'

include 'android'
include 'common'
include 'jvm'
include 'lint'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from files("../gradle/libs.versions.toml")
        }
    }
}
