rootProject.name = 'build-tools'

include 'common', 'lint'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from files("../gradle/libs.versions.toml")
        }
    }
}
